{
  "name": "black-glass",
  "version": "0.0.0",
  "private": true,
  "description": "BLACK GLASS — browser-first first-person immersive-sim thriller vertical slice (Babylon.js).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@babylonjs/core": "^7.39.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
